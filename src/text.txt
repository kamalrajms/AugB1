17/08/2026

## react intro

    What is React?
  

  
    1) React is a JavaScript library (not a framework) developed by Facebook (Meta) for building user interfaces (UIs), especially Single Page Applications (SPAs).

    2) It helps developers build reusable UI components that update efficiently when data changes.

    Instead of reloading a whole webpage when something changes, React updates only the parts of the UI that need to change.

    -------
    Why do we need React?

	It makes UI development faster, cleaner, and more efficient.

	Provides a better user experience with smooth updates without page reloads.


    ## must need to install node.js
 
## procedure to initiate project

    step 1 : npm create vite@latest [project_name]
    step 2 : select react
    step 3 : select javascript+ react compailer
    step 4 : npm install                // install node module packages
    step 5 : npm run dev 

    if you want to re-run your project mean 
        npm run dev


18/08/2026

## props

    Props is nothing but drilling data from parent to child components.

            declearing props
            Destructuring props

## Conditional rendering 

    Conditional rendering means showing or hiding  parts of UI based on some Condition

                ternary operator  {Condition ? true:false}
                logical operator  && || !

19/08/2026

## List rendering

    rendering multiple item dynamically using .map()


    array=[1,2,3,4]

    array.map((num)=>(
        num*2
    ))

    new array=[2,4,6,8]


## style in react

    global style -- index.css and app.css is called as global style.
    Local style -- to create a seperate css file for a component
    module style -- to implement this module style we need to create a seperate  file  
                    name as eg:[filename.module.css]
    inline style -- we need to declear style attribute with {{padding:"20px"}}
    object style -- we need to declear a styleing properties as an object 
                    in keys and value pair.



20/08/2026 && 21/08/2026

## usestate()

    usestate is a react hook that allows you to add state (data that can change over time)
    to a functional component.

    Normally, in js function ,variables are disappear after the function runs,
    But with usesate, React remember values b/w re-render.



    syntax:

    const name=5

    const [state,setState]=usestate(initialValue)
    const [count,setCount]=usestate(555)

    state-- the current value of the state variable.
    setState -- a function that update the state.
    initialValue -- the starting value of the state.


## useEffect()

    In react, useEffect is a hook that let you perform side useEffect
    in a function component.

    Side effect are task that happend outide the Normal React
    rendering process ,like

            Fetching data from an API
            updateing browser DOM manually
            Starting /Stopping timers.


    syntax:

            useEffect(()=>{
                // code
            },[count,dependency 2.... etc])

            [] = it will execute useEffect hook only once at befining.